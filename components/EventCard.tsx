import { Ionicons } from '@expo/vector-icons';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import type { CampusEvent } from '@/data/events';
type EventCardProps = { event: CampusEvent; onPress: () => void };
const categoryColors = { Academic: '#2563EB', Arts: '#9333EA', Sports: '#D97706' };
export function EventCard({ event, onPress }: EventCardProps) { return <Pressable accessibilityRole="button" onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.topRow}><View style={[styles.categoryBadge, { backgroundColor: `${categoryColors[event.category]}18` }]}><Text style={[styles.category, { color: categoryColors[event.category] }]}>{event.category}</Text></View>{event.joined && <Text style={styles.joined}>Joined</Text>}</View><Text style={styles.title}>{event.title}</Text><View style={styles.metaRow}><Ionicons color="#63758A" name="calendar-outline" size={16} /><Text style={styles.meta}>{event.dateTime}</Text></View><View style={styles.metaRow}><Ionicons color="#63758A" name="location-outline" size={16} /><Text style={styles.meta}>{event.venue}</Text></View><View style={styles.footer}><Text style={styles.availability}>{event.available} spots available</Text><Ionicons color="#1D4ED8" name="arrow-forward" size={18} /></View></Pressable>; }
const styles = StyleSheet.create({ availability: { color: '#1D4ED8', fontSize: 12, fontWeight: '700' }, card: { backgroundColor: '#FFFFFF', borderColor: '#DDE7F2', borderRadius: 16, borderWidth: 1, marginBottom: 14, padding: 18 }, category: { fontSize: 12, fontWeight: '800' }, categoryBadge: { borderRadius: 99, paddingHorizontal: 10, paddingVertical: 5 }, footer: { alignItems: 'center', borderTopColor: '#EDF2F7', borderTopWidth: 1, flexDirection: 'row', justifyContent: 'space-between', marginTop: 15, paddingTop: 13 }, joined: { color: '#15803D', fontSize: 12, fontWeight: '800' }, meta: { color: '#63758A', flex: 1, fontSize: 13, marginLeft: 8 }, metaRow: { alignItems: 'center', flexDirection: 'row', marginTop: 8 }, pressed: { opacity: 0.72, transform: [{ scale: 0.99 }] }, title: { color: '#14243A', fontSize: 18, fontWeight: '800', marginTop: 14 }, topRow: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between' } });
